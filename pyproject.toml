[project]
name = "data-platform-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django==6.0.6",
    "python-dotenv==1.2.2",
]

[dependency-groups]
dev = [
    "djlint==1.36.4",
    "django-debug-toolbar==6.3.0",
    "model-bakery==1.20",
    "pre-commit==4.6.0",
    "pytest==9.0.3",
    "pytest-cov==6.0",
    "pytest-django==4.9",
    "ruff==0.15.15",
]

[tool.ruff]
line-length = 99

[tool.uv]
exclude-newer = "7d"

[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # flake8-bugbear
    "B",
    # flake8-simplify
    "SIM",
    # isort
    "I",
]

[tool.ruff.lint.per-file-ignores]
"**/migrations/*.py" = ["E501"]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "data_platform_app.settings.test"
addopts = "-v --tb=auto --cov=data_platform_app --cov=users --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=90"
testpaths = ["tests"]

[tool.coverage.run]
omit = [
    "*/migrations/*",
    "data_platform_app/settings/*",
    "data_platform_app/wsgi.py",
    "data_platform_app/asgi.py",
    "manage.py",
]

[tool.djlint]
profile = "django"
indent = 2
max_line_length = 99
format_css = true
format_js = true
ignore = "H006,H021,H023,H025,H030,H031,T002"
